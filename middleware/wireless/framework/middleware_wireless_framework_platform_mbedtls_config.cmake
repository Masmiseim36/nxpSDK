# Add set(CONFIG_USE_middleware_wireless_framework_platform_mbedtls_config true) in config.cmake to use this component

include_guard(GLOBAL)
message("${CMAKE_CURRENT_LIST_FILE} component is included.")

